{
  "name": "alien-signal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute alien-signal-db --file=migrations/001_init.sql",
    "db:init:remote": "wrangler d1 execute alien-signal-db --remote --file=migrations/001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
